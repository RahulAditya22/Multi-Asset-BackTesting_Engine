# Offline fallback fixtures

These small CSV files are deterministic fallback fixtures for the demo when Yahoo Finance is unavailable or rate-limited. They are intentionally compact and are **not** presented as a source of real-time or investment-grade market history.

The engine normalizes them to the same OHLCV schema used for Yahoo Finance data.
